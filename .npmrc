# pnpm: fail on phantom dependencies (part of the "robust" story — §5)
strict-peer-dependencies=false
auto-install-peers=true
# Keep the store deterministic across CI + local
prefer-frozen-lockfile=true
